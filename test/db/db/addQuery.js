// Replace this with your target module file.

const mocha = require('mocha');
const assert = require('assert');
// const db = require('../../../db/db');

mocha.describe('Test cases for addQuery', () => {
    mocha.it('Test case ', () => {
        assert.equal(-1, [1, 2, 3].indexOf(4));
    });
});
