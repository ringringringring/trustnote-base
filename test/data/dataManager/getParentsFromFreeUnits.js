/* global describe it */

const assert = require('assert')
// const db = require('../../../db/db');

describe('data->dataManager->getParentsFromFreeUnits', () => {
    it('Test case for getParentsFromFreeUnits()', () => {
        assert.equal(-1, [1, 2, 3].indexOf(4))
    })
})
